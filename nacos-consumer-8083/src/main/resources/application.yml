server:
  port: 8083
spring:
  application:
    name: nacos-consumer
  cloud:
    discovery:
      server-addr: localhost:8848

#消费者将要去访问的服务名称(向nacos注册成功的微服务提供者)
service-url:
  nacos-user-service: http://nacos-provider
